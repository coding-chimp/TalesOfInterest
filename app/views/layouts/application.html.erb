<!DOCTYPE html>
<html>
<head>
  <title>TalesOfInterest</title>
  <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= javascript_include_tag "jwplayer" %>
  <%= javascript_include_tag "jwplayer.html5" %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
	<div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <a class="brand" href="/">Tales of Interest</a>
        <div class="nav-collapse">
          <ul class="nav">
            <% @podcasts = Podcast.order("name asc") %>
          	<% @podcasts.each do |podcast| %>
          		<li class="divider-vertical"></li>
          		<li><%= link_to podcast.name, podcast %></li>
          	<% end %> 
            <li class="divider-vertical"></li>
          </ul>
          <form class="navbar-search pull-right">
            <%= form_for @search do |f| %>
              <%= f.text_field :title_or_description_contains, :class => "search-query", :placeholder => "Search" %>
            <% end %>
          </form>
          <% @pages = Page.all %>
          <% unless @pages.size == 0 %>
          <ul class="nav pull-right">
            <% @pages.each do |page| %>
              <li class="divider-vertical"></li>
              <li><%= link_to page.titel, page %></li>
            <% end %>
            <li class="divider-vertical"></li>
          </ul>
          <% end %>
        </div>
      </div>
    </div>
  </div>
  <div class="container">
		<div class="row">
      <div class="span7">	<%= yield %></div>
			<div class="span4">
        <div class="well">
				  <h3>Subscribe</h3>
          <ul class="unstyled">
            <li><a href=""><%= image_tag("rss.png") %> Master Audio RSS Feed</a></li>
            <% @podcasts.each do |podcast| %>
              <li><a href=""><%= image_tag("itunes.png") %> <%= podcast.name %> in iTunes</a></li>
            <% end %> 
          </ul>
        </div>
        <div class="well">
          <h3>Spenden</h3>
          <p>Flattr Button</p>
        </div>
        <% @blogs = Blogroll.order("name asc") %>
        <% unless @blogs.size == 0 %>
          <div class="well">
            <h3>Blogroll</h3>
            <dl>
              <% @blogs.each do |blog| %>
                <dt><%= link_to blog.name, blog.url %></dt>
                <dd><%= blog.description %></dd>
              <% end %>
            </dl>
          </div>
        <% end %>
			</div>
		</div>
	</div>
  <div class="footer">
    <div class="container">
      <p>Copyright © 2010-<%= Date.today.strftime("%Y") %> Tales of Interest</p>
    </div>
  </div>
</body>
</html>