<% title "New #{@episode.podcast.name} Episode" %>

<div class="large-8 large-offset-1">
	<h2>New <%= @episode.podcast.name %> Episode</h2>
</div>

<%= simple_form_for @episode, url: { action: :create }, method: 'post' do |f| %>
	<%= render 'form', f: f %>
<% end %>

